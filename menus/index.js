const { collectionMenu } = require('./collection');
const { welcomeMenu } = require('./welcome');
const { dynamicMenu } = require('./dynamic');

module.exports = {
	collectionMenu,
	welcomeMenu,
	dynamicMenu,
};
